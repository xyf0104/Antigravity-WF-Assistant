# Antigravity WF助手 v1.6.0（Windows）

本次仅发布 Windows 安装包；macOS 版本保持 v1.5.9，不重新构建、不替换。

## 图片生成界面

- IDE 1.x、IDE 2.x 与 Antigravity 2.0 的已验证结构继续采用结构识别注入，不按单一版本号硬编码。
- Prompt 图片卡片保持自动展开，并显示实际生图模型；GPT Image 2 与原生 Gemini 图片链路不会互相冒充。
- 图片生成完成后只保留 Prompt 卡片中的图片，隐藏同一次生成结果在正文中追加的重复大图。
- 去重补丁升级到 v3：即使正文大图先挂载、Prompt 卡片后完成，也会按规范化后的同一 URI 清理已挂载重复项。
- 普通 Markdown 图片、不同 URI 图片以及未识别的未来组件结构保持不变。

## 模型与代理商管理

- 恢复代理商卡片默认收起、展开/收起模型、编辑代理商和整组测试连接。
- 编辑代理商时可刷新上游 `/models`，勾选决定注入模型；取消勾选只停用，不删除本地配置。
- 单模型外部只保留启停和推理强度编辑。
- 推理等级显示上游原始值：`auto`、`none`、`minimal`、`low`、`medium`、`high`、`xhigh`、`max`；每个模型只展示其实际支持的选项。
- 模型显示名附带代理商名称，例如 `gpt-5.6-sol（XIASS）`。

## 连接与启动性能

- 优先复用成功连接后保存的 Antigravity 安装路径，常规启动和重复连接不再反复等待 PowerShell、CIM 与注册表深度枚举。
- 文件指纹未变化时复用近期结构兼容性结论；程序或 bundle 变化后自动失效并重新验证。
- 首次首页在后台进行完整扫描，重复进入首页使用缓存；手动刷新仍执行完整发现。
- 连接成功立即显示简洁结果，详细文件列表不再占满弹窗；完整核验在后台更新。
- 点击“打开 Antigravity”会先关闭成功弹窗，再执行历史保护与启动。

## 版本与更新提醒

- 首页显示 Antigravity 产品真实版本，不再误用 VS Code OSS 内核版本。
- Antigravity 版本、安装路径或主程序发生变化时，强制提示重新连接补丁。
- 助手启动或从托盘重新打开时检查 GitHub 新版本，可一键下载、校验并启动 Windows 安装程序。

## 安全边界

- 不伪造 `generatedMedia` 上游字段，不全局替换未知 JavaScript。
- 未识别结构会安全跳过并显示原因。
- 安装状态缓存仅保存产品类型、安装路径、产品版本及 EXE 文件元数据，不包含账户、API Key、OAuth、Cookie、聊天记录或模型凭据。
