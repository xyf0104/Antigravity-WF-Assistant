# Antigravity WF助手 v1.4.19

`v1.4.19` 是当前正式发布版，基于已确认稳定的 `v1.4.6` 基线继续构建。`v1.4.6` 仍保留为稳定回退版本。

## 推理等级

- 为 GPT-5.6、DeepSeek V4 与已识别的 Claude 系列模型提供与其协议相匹配的离散推理等级选择。
- 未声明兼容推理等级的模型保持“自动”，不会向上游发送可能导致请求失败的未知等级。
- 手动添加模型、上游模型发现和账户池同步使用同一套推理等级规则；已保存模型的选择会被校验并安全归一化。

## 账户池与代理

- 账户同步不会覆盖已有模型的协议、上游地址或凭据绑定；同一模型可继续组合为账户池。
- 运行时会优先使用与模型协议相符的账户，避免将不兼容账户调度到当前请求。
- 更新检查和安装器版本字段已统一为 `1.4.19`。

## 标准安装包

- macOS Universal：`Antigravity-WF-Assistant-macOS-universal-v1.4.19-Installer.pkg`
- Windows x64：`Antigravity-WF-Assistant-Windows-x64-v1.4.19-Setup.exe`

发布页只提供上述两个标准安装包及同次发布的 `SHA256SUMS.txt`。
